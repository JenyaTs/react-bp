import { dynamicDate, dateFlag } from './dynamic-date-selector';
import { currentDate } from './current-date-selector';

export const date = dynamicDate;
export const additionalDate = currentDate;
export const flag = dateFlag;
